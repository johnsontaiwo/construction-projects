<%= form_for @contractor do |f| %>
 
  <% if @contractor.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@contractor.errors.count, "error") %> prohibited this from being saved:</h2>
        <ul>
         <% @contractor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

  <div class="field">
  <%= f.label :name %>
  <%= f.text_field :name.downcase %><br />
  </div>

  <div class="field">
  <%= f.label :address %>
  <%= f.text_area :address %>
  </div>

  <div class="field">
  <%= f.label :email %>
  <%= f.text_area :email %>
  </div>>
 
  <div class="field">
  <%= f.label :group %>
  <%= f.select "group", options_for_select(["Prime", "Sub"]), include_blank: true %></p>
  </div>
  
  <div class="actions"> 
  <%= f.submit %>
  </div>

<% end %>
